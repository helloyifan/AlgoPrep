class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        valid_options = [] # { remaining_word: "", wordDict []}

        while True:
            for valid_option in valid_options:
                
        return

    def doesFrontMatch(self, long_word, short_word):
        if longword[:len(short_word)]

sol = Solution()
s = "leetcode"
wordDict = ["leet","code"]

sol.wordBreak(s, wordDict)